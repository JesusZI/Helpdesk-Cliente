# Copy to .env and fill values for local testing or deployment
DB_HOST=localhost
DB_PORT=3306
DB_NAME=helpdesk
DB_USER=root
DB_PASS=

# When using the docker-compose provided above, these map to the `db` service.
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=helpdesk
MYSQL_USER=helpdesk_user
MYSQL_PASSWORD=helpdesk_pass
